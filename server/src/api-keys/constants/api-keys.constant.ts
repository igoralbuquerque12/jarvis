/** Maximum number of keys a single profile may hold. */
export const MAX_API_KEYS_PER_PROFILE = 10;

/** Where the guard stores the resolved profile on the Express request. */
export const API_KEY_PROFILE_REQUEST_KEY = 'apiKeyProfile';
